# GitHub Pages Jekyll configuration
# This file tells GitHub Pages how to serve this site

title: "ME Portfolio"
description: "Mechanical Engineering Portfolio"

# Use default Jekyll theme (or remove for plain HTML)
theme: null

# Exclude files from the build
exclude:
  - README.md
  - "*.sh"
